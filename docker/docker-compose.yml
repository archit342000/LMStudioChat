name: my-ai

services:
  app:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    networks:
      - secure-internal
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      - tavily_mcp
      - playwright_mcp
      - inference-proxy
    volumes:
      - /home/archit-anand/docker-data/shared/My-AI-data:/app/backend/data
      - ..:/app
    environment:
      - TZ=Asia/Kolkata
      - AI_PROXY_URL=http://inference-proxy:5001
      - CODE_RUNNER_URL=http://code_runner_mcp:8100
    secrets:
      - DATA_DIR
      - APP_PASSWORD
      - AI_URL
      - AI_API_KEY
      - EMBEDDING_URL
      - EMBEDDING_API_KEY
      - TAVILY_API_KEY
      - MCP_API_KEY
      - PLAYWRIGHT_MCP_API_KEY
      - HF_TOKEN
      - CODE_RUNNER_API_KEY

  bastion_ssh:
    build:
      context: ..
      dockerfile: docker/Dockerfile.bastion
    container_name: bastion_ssh
    volumes:
      - ../secrets/authorized_keys:/home/appuser/.ssh/authorized_keys:ro
    ports:
      - 2222:2222
    networks:
      - secure-internal
    restart: unless-stopped

  tavily_mcp:
    build:
      context: ..
      dockerfile: docker/Dockerfile.mcp
    container_name: tavily_mcp
    networks:
      - secure-internal
    volumes:
      - ../tavily_mcp:/app/tavily_mcp
    environment:
      - TZ=Asia/Kolkata
    secrets:
      - TAVILY_API_KEY
      - MCP_API_KEY
    restart: unless-stopped

  playwright_mcp:
    build:
      context: ..
      dockerfile: docker/Dockerfile.playwright
    container_name: playwright_mcp
    networks:
      - secure-internal
    volumes:
      - ../playwright_mcp:/app/playwright_mcp
      - /home/archit-anand/docker-data/shared/My-AI-playwright-data:/app/playwright_data
    environment:
      - TZ=Asia/Kolkata
    secrets:
      - PLAYWRIGHT_MCP_API_KEY
    command: ["/app/playwright_mcp/entrypoint.sh"]
    restart: unless-stopped

  inference-proxy:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: inference-proxy
    ports:
      - "5001:5001"
    networks:
      - secure-internal
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ..:/app
    environment:
      - TZ=Asia/Kolkata
      - PYTHONUNBUFFERED=1
    secrets:
      - AI_URL
      - AI_API_KEY
      - EMBEDDING_URL
      - EMBEDDING_API_KEY
    command: ["python3", "inference_proxy/app.py"]
    restart: unless-stopped

  code_runner_mcp:
    build:
      context: ..
      dockerfile: docker/Dockerfile.code-runner
    networks:
      - secure-internal
    volumes:
      - ../code_runner_mcp:/app
    secrets:
      - CODE_RUNNER_API_KEY
      - MYSQL_HOST
      - MYSQL_USER
      - MYSQL_PASSWORD
    environment:
      - API_KEY_FILE=/run/secrets/CODE_RUNNER_API_KEY
      - MYSQL_HOST_FILE=/run/secrets/MYSQL_HOST
      - MYSQL_USER_FILE=/run/secrets/MYSQL_USER
      - MYSQL_PASSWORD_FILE=/run/secrets/MYSQL_PASSWORD
    deploy:
      resources:
        limits:
          memory: ${CODE_RUNNER_MEMORY_LIMIT:-512M}
          cpus: "${CODE_RUNNER_CPU_LIMIT:-1.0}"
    restart: unless-stopped

  mysql_db:
    image: mysql:8.0
    networks:
      - secure-internal
    secrets:
      - MYSQL_ROOT_PASSWORD
      - MYSQL_PASSWORD
      - MYSQL_USER
    environment:
      - MYSQL_ROOT_PASSWORD_FILE=/run/secrets/MYSQL_ROOT_PASSWORD
      - MYSQL_DATABASE=sandbox
      - MYSQL_USER_FILE=/run/secrets/MYSQL_USER
      - MYSQL_PASSWORD_FILE=/run/secrets/MYSQL_PASSWORD
    volumes:
      - mysql_data:/var/lib/mysql
    deploy:
      resources:
        limits:
          memory: 256M
    restart: unless-stopped

networks:
  secure-internal:
    driver: bridge

volumes:
  mysql_data:

secrets:
  # Defining secrets mapped to files in the ../secrets directory
  DATA_DIR:
    file: ../secrets/DATA_DIR
  APP_PASSWORD:
    file: ../secrets/APP_PASSWORD
  AI_URL:
    file: ../secrets/AI_URL
  AI_API_KEY:
    file: ../secrets/AI_API_KEY
  EMBEDDING_URL:
    file: ../secrets/EMBEDDING_URL
  EMBEDDING_API_KEY:
    file: ../secrets/EMBEDDING_API_KEY
  TAVILY_API_KEY:
    file: ../secrets/TAVILY_API_KEY
  MCP_API_KEY:
    file: ../secrets/MCP_API_KEY
  PLAYWRIGHT_MCP_API_KEY:
    file: ../secrets/PLAYWRIGHT_MCP_API_KEY
  HF_TOKEN:
    file: ../secrets/HF_TOKEN
  CODE_RUNNER_API_KEY:
    file: ../secrets/CODE_RUNNER_API_KEY
  MYSQL_ROOT_PASSWORD:
    file: ../secrets/MYSQL_ROOT_PASSWORD
  MYSQL_HOST:
    file: ../secrets/MYSQL_HOST
  MYSQL_USER:
    file: ../secrets/MYSQL_USER
  MYSQL_PASSWORD:
    file: ../secrets/MYSQL_PASSWORD
